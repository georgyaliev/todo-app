# Переменные окружения для Todo App
# Скопируй этот файл в .env и заполни значения (не коммить .env в git)

# Пример переменной окружения для Vite (доступна в коде как import.meta.env.VITE_*)
# VITE_APP_TITLE=Список задач
